# Add directories or file patterns to ignore during indexing (e.g. foo/ or *.csv)
node_modules/
dist/
build/

# Environment files
!.env.development
!.env.staging
!.env.production
!.env

# Yarn
!.yarn/