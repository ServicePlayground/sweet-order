name: Deploy Backend to EC2 (Staging)

permissions:
  contents: read

on:
  push:
    tags:
      - "backend/staging-*"

jobs:
  deploy-staging:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Enable Corepack
        run: corepack enable

      - name: Setup Yarn
        run: corepack prepare yarn@4.9.4 --activate

      - name: Install dependencies
        run: yarn install

      - name: Build backend
        run: yarn run backend:build:staging

      - name: Prepare deployment package
        run: |
          # 빌드 산출물과 의존성 파일만 포함하는 배포 패키지 생성 (node_modules 제외)
          # 백엔드만 독립적으로 배포 (workspace 구조 제거)
          mkdir -p /tmp/deploy-package/apps/backend/src/infra/database/prisma
          
          # 백엔드 빌드 산출물 및 package.json 복사
          echo "📦 백엔드 빌드 산출물 복사 중..."
          cp -r apps/backend/dist /tmp/deploy-package/apps/backend/
          cp apps/backend/package.json /tmp/deploy-package/apps/backend/
          
          # Prisma schema 및 migrations 복사 (prisma migrate deploy에 필요)
          echo "📦 Prisma schema 및 migrations 복사 중..."
          cp apps/backend/src/infra/database/prisma/schema.prisma /tmp/deploy-package/apps/backend/src/infra/database/prisma/
          if [ -d "apps/backend/src/infra/database/prisma/migrations" ]; then
            cp -r apps/backend/src/infra/database/prisma/migrations /tmp/deploy-package/apps/backend/src/infra/database/prisma/
          fi
                    
          # 압축
          cd /tmp
          tar -czf deploy.tar.gz -C deploy-package .
          mv deploy.tar.gz ${{ github.workspace }}/

      - name: Copy deployment package to EC2
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.STAGING_EC2_HOST }}
          username: ec2-user
          key: ${{ secrets.STAGING_EC2_SSH_KEY }}
          source: "deploy.tar.gz"
          target: "~/"

      - name: Deploy to EC2
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.STAGING_EC2_HOST }}
          username: ec2-user
          key: ${{ secrets.STAGING_EC2_SSH_KEY }}
          script: |
            set -Ee  # 함수 내부 에러도 trap

            # 디스크 사용량 계산 함수 (한 번만 계산)
            get_disk_usage() {
              df / | awk 'END {gsub("%","",$5); print $5}'
            }

            # 롤백 함수 정의
            rollback_deployment() {
              echo ""
              echo "🔄 배포 실패로 인한 롤백 시작..."
              
              pm2 delete sweet-order-backend 2>/dev/null || true
              
              LATEST_BACKUP=$(ls -td ~/sweet-order.backup.* 2>/dev/null | head -1)
              
              if [ -n "$LATEST_BACKUP" ] && [ -d "$LATEST_BACKUP" ]; then
                echo "📦 백업 복원 중: $LATEST_BACKUP"
                rm -rf ~/sweet-order
                mv "$LATEST_BACKUP" ~/sweet-order
                
                if [ -f ~/sweet-order/apps/backend/dist/main.js ]; then
                  cd ~/sweet-order/apps/backend
                  NODE_ENV=staging pm2 start dist/main.js \
                    --name sweet-order-backend \
                    --update-env \
                    --cwd $(pwd)
                  pm2 save
                  echo "✅ 롤백 완료"
                else
                  echo "⚠️ 롤백 실패 - 백업에 빌드 파일이 없습니다"
                fi
              else
                echo "❌ 롤백 실패 - 백업을 찾을 수 없습니다"
              fi
              
              exit 1
            }

            # 배포 실패 시 롤백을 위한 trap 설정 (맨 위에서 설정하여 전체 구간 보호)
            trap rollback_deployment ERR

            # 백업 파일 개수 제한 함수 (디스크 사용량에 따라 동적 조정)
            # 보조 수단으로만 사용: 디스크 부족 시와 배포 성공 후에만 실행
            cleanup_old_backups() {
              local BACKUP_TYPE=$1
              local BACKUP_PATTERN=$2
              local USE_PERCENT=$3
              local KEEP_COUNT=5
              
              # 디스크 사용량에 따라 백업 개수 조정
              if [ "$USE_PERCENT" -ge 90 ]; then
                KEEP_COUNT=2
                echo "⚠️ 디스크 사용량이 높아 백업을 최근 ${KEEP_COUNT}개만 유지합니다"
              elif [ "$USE_PERCENT" -ge 85 ]; then
                KEEP_COUNT=3
                echo "⚠️ 디스크 사용량이 높아 백업을 최근 ${KEEP_COUNT}개만 유지합니다"
              fi
              
              echo "🧹 오래된 ${BACKUP_TYPE} 백업 정리 중 (최근 ${KEEP_COUNT}개만 유지)..."
              
              # 안전한 백업 삭제: 경로 검증 강화
              local HOME_DIR=$(eval echo ~)
              local count=0
              
              # 백업 파일 목록을 임시 파일에 저장하여 안전하게 처리
              local temp_list=$(mktemp)
              ls -t ${BACKUP_PATTERN} 2>/dev/null | tail -n +$((KEEP_COUNT + 1)) > "$temp_list" || true
              
              while IFS= read -r old_backup || [ -n "$old_backup" ]; do
                # 경로 검증: 홈 디렉토리 내 백업만 삭제, 빈 줄 무시
                [ -z "$old_backup" ] && continue
                
                # 홈 디렉토리 경로로 확장
                local expanded_path=$(eval echo "$old_backup")
                
                # 홈 디렉토리 내에 있고 실제 존재하는 파일/디렉토리만 삭제
                if [[ "$expanded_path" == "$HOME_DIR"* ]] && [ -e "$expanded_path" ]; then
                  echo "  삭제: $old_backup"
                  rm -rf "$expanded_path"
                  count=$((count + 1))
                fi
              done < "$temp_list"
              
              rm -f "$temp_list"
              
              if [ $count -eq 0 ]; then
                echo "  삭제할 백업이 없습니다"
              fi
            }

            # 파일/디렉토리 용량 출력 함수
            show_disk_usage_details() {
              echo ""
              echo "📊 상세 디스크 사용량 정보"
              echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
              
              # 전체 디스크 사용량
              echo "💾 전체 디스크 사용량:"
              df -h / | tail -1 | awk '{print "  사용: " $3 " / " $2 " (" $5 " 사용됨)"}'
              echo ""
              
              # 홈 디렉토리 전체 용량
              if [ -d ~ ]; then
                echo "🏠 홈 디렉토리 (~/) 용량:"
                du -sh ~ 2>/dev/null | awk '{print "  " $1}'
                echo ""
              fi
              
              # sweet-order 프로젝트 디렉토리 용량
              if [ -d ~/sweet-order ]; then
                echo "📁 sweet-order 프로젝트 전체 용량:"
                du -sh ~/sweet-order 2>/dev/null | awk '{print "  " $1}'
                echo ""
                
                # dist 디렉토리 용량
                if [ -d ~/sweet-order/apps/backend/dist ]; then
                  echo "📦 dist 디렉토리 용량:"
                  du -sh ~/sweet-order/apps/backend/dist 2>/dev/null | awk '{print "  " $1}'
                  echo ""
                fi
                
                # node_modules 용량
                if [ -d ~/sweet-order/apps/backend/node_modules ]; then
                  echo "📚 node_modules 디렉토리 용량:"
                  du -sh ~/sweet-order/apps/backend/node_modules 2>/dev/null | awk '{print "  " $1}'
                  echo ""
                fi
              fi
              
              # 백업 파일들 용량
              echo "💾 백업 파일들 용량:"
              local backup_total=0
              if ls ~/sweet-order.backup.* 2>/dev/null | head -5 > /dev/null; then
                for backup in $(ls -td ~/sweet-order.backup.* 2>/dev/null | head -5); do
                  if [ -d "$backup" ]; then
                    local size=$(du -sh "$backup" 2>/dev/null | awk '{print $1}')
                    local name=$(basename "$backup")
                    echo "  $name: $size"
                  fi
                done
              else
                echo "  백업 파일 없음"
              fi
              echo ""
              
              # DB 백업 파일들 용량
              echo "🗄️ DB 백업 파일들 용량:"
              if ls ~/db-backup.*.dump 2>/dev/null | head -5 > /dev/null; then
                for db_backup in $(ls -t ~/db-backup.*.dump 2>/dev/null | head -5); do
                  if [ -f "$db_backup" ]; then
                    local size=$(du -h "$db_backup" 2>/dev/null | awk '{print $1}')
                    local name=$(basename "$db_backup")
                    echo "  $name: $size"
                  fi
                done
              else
                echo "  DB 백업 파일 없음"
              fi
              echo ""
              
              # 큰 파일 상위 10개 (홈 디렉토리 내)
              echo "📋 홈 디렉토리 내 큰 파일 상위 10개:"
              find ~ -type f -size +10M 2>/dev/null | xargs du -h 2>/dev/null | sort -rh | head -10 | awk '{print "  " $1 " - " $2}' || echo "  10MB 이상 파일 없음"
              echo ""
              
              echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            }

            # 디스크 사용량 체크 (한 번만 계산)
            USE_PERCENT=$(get_disk_usage)
            echo "📊 디스크 사용량: ${USE_PERCENT}%"
            
            # 상세 용량 정보 출력
            show_disk_usage_details

            # 디스크 부족 시에만 정리 작업 실행 (보조 수단)
            if [ "$USE_PERCENT" -gt 80 ]; then
              echo "⚠️ 디스크 사용량이 높습니다. 정리 작업 시작..."
              
              cleanup_old_backups "서버" "~/sweet-order.backup.*" "$USE_PERCENT"
              cleanup_old_backups "DB" "~/db-backup.*.dump" "$USE_PERCENT"
              
              # PM2 로그 정리만 수행 (실행 중인 프로세스 파일은 건드리지 않음)
              pm2 flush || true
              
              # 정리 후 재확인
              USE_PERCENT=$(get_disk_usage)
              echo "정리 후 디스크 사용량: ${USE_PERCENT}%"
              
              if [ "$USE_PERCENT" -gt 95 ]; then
                echo "❌ 디스크 사용량 ${USE_PERCENT}% — 공간 부족. 배포 중단"
                exit 1
              fi
            fi

            # 🗄️ DB 백업 생성 (배포 전)
            # Custom format 사용 (.dump 확장자)
            echo "🗄️ 데이터베이스 백업 생성 중..."
            DB_BACKUP_NAME=~/db-backup.$(date +%Y%m%d_%H%M%S).dump
            PGPASSWORD='${{ secrets.STAGING_DATABASE_PASSWORD }}' \
              pg_dump -h localhost -p 5432 -U sweetorder_admin \
              -d sweetorder_staging_db \
              -Fc -f "$DB_BACKUP_NAME" 2>/dev/null || {
              echo "⚠️ DB 백업 실패 (계속 진행합니다)"
              rm -f "$DB_BACKUP_NAME"
            }

            if [ -f "$DB_BACKUP_NAME" ]; then
              echo "✅ DB 백업 완료: $DB_BACKUP_NAME"
            fi

            # 기존 프로젝트 백업
            if [ -d ~/sweet-order ]; then
              CURRENT_BACKUP_NAME=~/sweet-order.backup.$(date +%Y%m%d_%H%M%S)
              echo "📦 기존 프로젝트 백업 중: $CURRENT_BACKUP_NAME"
              mv ~/sweet-order "$CURRENT_BACKUP_NAME" || {
                echo "⚠️ 백업 실패, 기존 디렉토리 삭제 후 진행"
                rm -rf ~/sweet-order
              }
            fi

            mkdir -p ~/sweet-order
            cd ~/sweet-order

            echo "📂 배포 패키지 압축 해제"
            tar -xzf ~/deploy.tar.gz
            rm ~/deploy.tar.gz

            if [ ! -d "apps/backend/dist" ]; then
              echo "❌ 빌드 산출물이 없습니다"
              rollback_deployment
            fi

            # 압축 해제 후 용량 정보 출력
            echo ""
            echo "📊 배포 패키지 압축 해제 후 용량:"
            if [ -d "apps/backend/dist" ]; then
              echo "  dist 디렉토리: $(du -sh apps/backend/dist 2>/dev/null | awk '{print $1}')"
            fi
            if [ -f "apps/backend/package.json" ]; then
              echo "  package.json: $(du -h apps/backend/package.json 2>/dev/null | awk '{print $1}')"
            fi
            echo ""

            # Node.js 및 Yarn 확인
            if ! command -v node &> /dev/null; then
              echo "❌ Node.js가 설치되어 있지 않습니다"
              exit 1
            fi

            # Yarn 설치 확인 및 설정
            if ! command -v yarn &> /dev/null; then
              echo "📦 Yarn 설치 중..."
              corepack enable || npm install -g corepack
              corepack prepare yarn@4.9.4 --activate
            fi

            # 백엔드 디렉토리로 이동하여 의존성 설치
            cd apps/backend
            
            echo "📦 의존성 설치 중 (node_modules 생성)..."
            yarn install || {
              echo "❌ yarn install 실패"
              rollback_deployment
            }
            
            # yarn install 후 용량 정보 출력
            echo ""
            echo "📊 yarn install 후 용량:"
            if [ -d "node_modules" ]; then
              echo "  node_modules: $(du -sh node_modules 2>/dev/null | awk '{print $1}')"
            fi
            echo ""
            
            cd ~/sweet-order


            echo "⚙️ 환경 변수 파일 생성 중..."
            cat > apps/backend/.env.staging << EOF
            # Environment
            NODE_ENV=staging

            # PORT
            PORT=8080

            # DOMAIN
            PUBLIC_USER_DOMAIN=${{ secrets.STAGING_PUBLIC_USER_DOMAIN }}
            PUBLIC_SELLER_DOMAIN=${{ secrets.STAGING_PUBLIC_SELLER_DOMAIN }}
            PUBLIC_ADMIN_DOMAIN=${{ secrets.STAGING_PUBLIC_ADMIN_DOMAIN }}

            # CORS Configuration
            CORS_ORIGIN=${{ secrets.STAGING_CORS_ORIGIN }}

            # JWT
            JWT_SECRET=${{ secrets.STAGING_JWT_SECRET }}

            # DATABASE
            # 옵션 1: EC2 PostgreSQL 사용 (비용 절감)
            DATABASE_URL=postgresql://sweetorder_admin:${{ secrets.STAGING_DATABASE_PASSWORD }}@localhost:5432/sweetorder_staging_db?schema=public

            # GOOGLE OAuth
            GOOGLE_CLIENT_ID=${{ secrets.STAGING_GOOGLE_CLIENT_ID }}
            GOOGLE_CLIENT_SECRET=${{ secrets.STAGING_GOOGLE_CLIENT_SECRET }}
            GOOGLE_REDIRECT_URI=/auth/login/google

            # SWAGGER
            SWAGGER_USERNAME=${{ secrets.STAGING_SWAGGER_USERNAME }}
            SWAGGER_PASSWORD=${{ secrets.STAGING_SWAGGER_PASSWORD }}

            # NTS API
            NTS_API_URL=https://api.odcloud.kr/api
            KFTC_API_URL=https://apis.data.go.kr
            DATA_GO_KR_API_KEY=${{ secrets.STAGING_DATA_GO_KR_API_KEY }}

            # AWS S3
            AWS_ACCESS_KEY_ID=${{ secrets.AWS_ACCESS_KEY_ID }}
            AWS_SECRET_ACCESS_KEY=${{ secrets.AWS_SECRET_ACCESS_KEY }}
            AWS_REGION=ap-northeast-2
            S3_BUCKET=${{ secrets.STAGING_S3_BUCKET }}
            CLOUDFRONT_DOMAIN=${{ secrets.STAGING_CLOUDFRONT_DOMAIN }}
            EOF

            # 환경 변수 파일 권한 설정
            chmod 600 apps/backend/.env.staging

            echo "🗄️ Prisma migrate 실행"
            cd apps/backend
            DATABASE_URL=$(grep '^DATABASE_URL=' .env.staging | head -1 | cut -d '=' -f2- | tr -d '\r\n' | sed 's/^[[:space:]]*//;s/[[:space:]]*$//')
            export DATABASE_URL

            if ! npx prisma migrate deploy --schema ./src/infra/database/prisma/schema.prisma; then
              echo "❌ Prisma migrate 실패"
              rollback_deployment
            fi

            cd ~/sweet-order/apps/backend

            echo "🚀 PM2 restart"
            pm2 delete sweet-order-backend || true
            NODE_ENV=staging pm2 start dist/main.js \
              --name sweet-order-backend \
              --update-env \
              --cwd $(pwd) || {
              echo "❌ PM2 시작 실패"
              rollback_deployment
            }
            pm2 save

            trap - ERR

            echo "✅ 배포 완료!"
            pm2 list

            # 배포 성공 후 용량 정보 출력
            echo ""
            echo "📊 배포 후 디스크 사용량:"
            show_disk_usage_details

            # 배포 성공 후 백업 정리 (1회만 실행)
            USE_PERCENT=$(get_disk_usage)
            cleanup_old_backups "서버" "~/sweet-order.backup.*" "$USE_PERCENT"
            cleanup_old_backups "DB" "~/db-backup.*.dump" "$USE_PERCENT"
            
            # 정리 후 최종 용량 정보 출력
            echo ""
            echo "📊 정리 후 최종 디스크 사용량:"
            USE_PERCENT=$(get_disk_usage)
            echo "📊 디스크 사용량: ${USE_PERCENT}%"
            show_disk_usage_details
